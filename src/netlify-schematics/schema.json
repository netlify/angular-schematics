{
  "$schema": "http://json-schema.org/schema",
  "id": "NetlifySchematicsNgAdd",
  "title": "ng add schematic",
  "type": "object",
  "description": "Creates Netlify config files and edits the gitignore file 🔒",
  "properties": {
    "command": {
      "type": "string",
      "description": "Default build command.",
      "x-prompt": "What is 🧱 your project's build command? (maybe `ng build --prod`)"
    },
    "publish": {
      "type": "string",
      "description": "The project directory.",
      "x-prompt": "What 🗂  directory holds your built project? (maybe `dist/project-name`)"
    },
    "apiId": {
      "type": "string",
      "description": "Netlify user API ID.",
      "x-prompt": "What is your Netlify 📟  API ID?"
    },
    "accessToken": {
      "type": "string",
      "description": "Netlify user Person Access Token.",
      "x-prompt": "What is your Netlify 🔑  Personal Access Token?"
    },
    "projectName": {
      "type": "string",
      "description": "Netlify project name.",
      "x-prompt": "What is your 📝  project name?"
    }
  }
}
